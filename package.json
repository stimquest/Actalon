{
  "name": "actalon-notaire",
  "version": "1.0.0",
  "description": "Site web pour l'étude notariale Actalon",
  "main": "index.js",
  "scripts": {
    "start": "npx esbuild index.tsx --bundle --outfile=dist/index.js --servedir=dist --loader:.tsx=jsx --define:process.env.API_KEY='\"\"' --watch",
    "build": "rm -rf dist && mkdir dist && cp index.html dist/index.html && cp -r public dist/public || true && npx esbuild index.tsx --bundle --minify --outfile=dist/index.js --loader:.tsx=jsx --define:process.env.NODE_ENV='\"production\"' --define:process.env.API_KEY='\"'${API_KEY}'\"'"
  },
  "dependencies": {
    "@google/genai": "^1.30.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}